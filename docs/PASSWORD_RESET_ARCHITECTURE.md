════════════════════════════════════════════════════════════════════════════════
  PASSWORD RESET FEATURE - VISUAL FLOW DIAGRAMS & ARCHITECTURE
════════════════════════════════════════════════════════════════════════════════

🎯 SMART DETECTION FLOW
──────────────────────────────────────────────────────────────────────────────

                          ┌─────────────────────┐
                          │   User Forgot Pass  │
                          └──────────┬──────────┘
                                     │
                                     ▼
                          ┌─────────────────────┐
                          │  Click "Lupa Pwd?"  │
                          │  (from login page)  │
                          └──────────┬──────────┘
                                     │
                                     ▼
                          ┌─────────────────────┐
                          │  forgot-password.php│
                          │  Input: Email only  │
                          └──────────┬──────────┘
                                     │
                     ┌───────────────┴───────────────┐
                     │                               │
                     ▼ Query DB                      ▼
          ┌──────────────────────┐        ┌──────────────────────┐
          │ login_method=manual? │        │ login_method=google? │
          │   (Email found)      │        │   (Email found)      │
          └──────────┬───────────┘        └──────────┬───────────┘
                     │                              │
                  YES│                           YES│
                     │                              │
      ┌──────────────▼─────────────┐  ┌─────────────▼──────────────┐
      │  Generate Random Token     │  │  Show Google OAuth Message │
      │  - 32 bytes = 64 hex chars │  │  "Akun Anda login via      │
      │  - Expires in 1 hour       │  │   Google. Gunakan tombol   │
      │                            │  │   Masuk dengan Google"     │
      │  Send Email with:          │  │                            │
      │  - Reset link + token      │  │  Buttons:                  │
      │  - User's name             │  │  [Kembali] [Daftar Baru]   │
      │  - Expiry info             │  └────────────────────────────┘
      │                            │
      │  Show Success Message:     │
      │  "Email telah dikirim"     │
      └──────────┬────────────────┘
                 │
                 ▼ User clicks email link
      ┌──────────────────────────────────┐
      │  reset-password.php?token=XXXXXX │
      │  - Validate token exists         │
      │  - Check if not expired          │
      │  - Show reset form               │
      └──────────┬───────────────────────┘
                 │
                 ▼ User submits new password
      ┌──────────────────────────────────┐
      │  Validate & Hash Password        │
      │  - Min 6 chars                   │
      │  - bcrypt hashing                │
      │  - Update DB                     │
      │  - Clear reset_token             │
      └──────────┬───────────────────────┘
                 │
                 ▼
      ┌──────────────────────────────────┐
      │  Show Success & Redirect          │
      │  "Password berhasil direset"      │
      │  [Masuk Sekarang] → login.php    │
      └──────────────────────────────────┘


════════════════════════════════════════════════════════════════════════════════

📊 DATABASE INTERACTION DIAGRAM
──────────────────────────────────────────────────────────────────────────────

┌─ USERS TABLE ────────────────────────────────────────────┐
│                                                           │
│  user_id │ email          │ password_hash │ login_method │
│  ─────────────────────────────────────────────────────   │
│     1    │ test@email.com │ $2y$10$...   │   'manual'   │
│     2    │ user@gmail.com │ $2y$10$...   │   'google'   │
│                                                           │
│  reset_token (NEW)        │ reset_token_expires (NEW)   │
│  ─────────────────────────┼─────────────────────────   │
│  a1b2c3d4... (when resetting) │ 2026-01-21 10:30:00   │
│  NULL (normally)          │ NULL (normally)           │
│                                                           │
└───────────────────────────────────────────────────────────┘

┌─ TRANSACTION FLOW ──────────────────────────────────────┐
│                                                         │
│  1. User submits email                                  │
│     ↓                                                   │
│  2. Query: SELECT login_method FROM users WHERE email  │
│     ↓                                                   │
│  3a. If 'manual':                                       │
│      - Generate token                                   │
│      - UPDATE users SET reset_token, expires           │
│      - Send email with link                            │
│     ↓                                                   │
│  3b. If 'google':                                       │
│      - Show message (no DB update)                      │
│     ↓                                                   │
│  4. User clicks email link                             │
│     ↓                                                   │
│  5. Query: SELECT user_id FROM users                   │
│           WHERE reset_token = ? AND expires > NOW()    │
│     ↓                                                   │
│  6. If valid:                                           │
│      - Show password form                               │
│      - User submits new password                        │
│      - UPDATE users SET password, clear token          │
│     ↓                                                   │
│  7. Redirect to login with success message             │
│                                                         │
└─────────────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════════════════════

🏗️ FILE ARCHITECTURE
──────────────────────────────────────────────────────────────────────────────

pages/auth/
│
├── login.php
│   └─ "Lupa Password?" → links to forgot-password.php
│
├── forgot-password.php (NEW)
│   ├─ GET: Shows email form
│   ├─ POST: Processes email input
│   │   ├─ Validates email format
│   │   ├─ Queries database for login_method
│   │   ├─ If 'manual': 
│   │   │   ├─ Generate token
│   │   │   ├─ Update database
│   │   │   └─ Send email
│   │   └─ If 'google': Show message
│   └─ Email function: sendEmail()
│
├── reset-password.php (NEW)
│   ├─ GET: Validate token from URL param
│   │   ├─ Query DB for valid token
│   │   ├─ Check expiry time
│   │   └─ Show form if valid
│   └─ POST: Process password reset
│       ├─ Validate password
│       ├─ Hash password (bcrypt)
│       ├─ Update DB
│       ├─ Clear reset token
│       └─ Show success message
│
├── register.php (MODIFIED)
│   └─ On user creation: SET login_method = 'manual'
│
└── google-callback.php (MODIFIED)
    └─ On user creation: SET login_method = 'google'


════════════════════════════════════════════════════════════════════════════════

🔒 SECURITY LAYERS
──────────────────────────────────────────────────────────────────────────────

LAYER 1: Token Security
  ├─ Generation: bin2hex(random_bytes(32))
  │   └─ 32 bytes = 256 bits of entropy
  │   └─ Hex encoded = 64 character string
  │   └─ Impossible to guess
  │
  ├─ Storage: In database (not hashed)
  │   └─ Ok to not hash (single-use, time-limited)
  │
  └─ Expiry: 1 hour (timestamp in DB)
     └─ Prevents old tokens from working

LAYER 2: Password Security
  ├─ Hash: bcrypt (via Database::hashPassword())
  │   └─ One-way encryption
  │   └─ Salt built-in
  │   └─ Cost factor: 10 (configurable)
  │
  └─ Validation: Min 6 chars
     └─ Frontend + Backend validation

LAYER 3: Database Security
  ├─ Prepared Statements: All queries use ?
  │   └─ Prevents SQL injection
  │
  └─ Method Detection: login_method column
     └─ Prevents incorrect reset for Google users

LAYER 4: Email Security
  ├─ Token in URL: Single-use link
  │   └─ User must have email access
  │
  ├─ HTTPS Recommended: For production
  │   └─ Protects token in transit
  │
  └─ No Sensitive Data: Message doesn't expose info
     └─ Same response for valid/invalid emails

LAYER 5: Session Security
  ├─ Token Cleanup: Removed after use
  │   └─ Can't reuse same token
  │
  └─ Expiry Verification: Always checked
     └─ Expired tokens rejected


════════════════════════════════════════════════════════════════════════════════

📱 RESPONSE STATES
──────────────────────────────────────────────────────────────────────────────

FORGOT PASSWORD PAGE:

┌─ If Email Not Found ────────────────────────────────┐
│ Message: "Jika email terdaftar, kami akan mengirim" │
│ Status: SUCCESS (for security)                      │
│ Action: No email sent                               │
└─────────────────────────────────────────────────────┘

┌─ If Manual User ────────────────────────────────────┐
│ Message: "Email reset telah dikirim ke: xxx@email"  │
│ Status: SUCCESS                                     │
│ Action: Email + token sent                          │
└─────────────────────────────────────────────────────┘

┌─ If Google User ────────────────────────────────────┐
│ Message: "Akun Anda login via Google..."            │
│ Status: INFO (blue box)                             │
│ Action: No email sent                               │
│ Buttons: [Kembali] [Daftar Akun Baru]              │
└─────────────────────────────────────────────────────┘

RESET PASSWORD PAGE:

┌─ If Token Valid ─────────────────────────────────┐
│ Status: Show form                                │
│ Form: Email (read-only) + New Password          │
│ Button: [Reset Password]                        │
└──────────────────────────────────────────────────┘

┌─ If Token Invalid/Expired ──────────────────────┐
│ Message: "Link tidak valid atau sudah kadaluarsa"│
│ Status: ERROR (red box)                         │
│ Buttons: [Kembali ke Login]                     │
└──────────────────────────────────────────────────┘

┌─ If Reset Successful ──────────────────────────┐
│ Message: "Password berhasil direset!"           │
│ Status: SUCCESS (green box)                     │
│ Button: [Masuk Sekarang] → login.php           │
└──────────────────────────────────────────────────┘


════════════════════════════════════════════════════════════════════════════════

⏱️ TIMING DIAGRAM
──────────────────────────────────────────────────────────────────────────────

Timeline for Manual User Reset:

  T0: User clicks "Lupa Password?"
      └─ forgot-password.php loads
         │
  T0+1s: User enters email & submits
      └─ Email validation
      └─ Database query
      └─ Token generated: abc123def456...
      └─ Token expires at: T0 + 1 hour
      └─ Email queued/sent
         │
  T0+2m: User receives email with link
      └─ Link: reset-password.php?token=abc123def456...
         │
  T0+30m: User clicks link (within 1 hour)
      └─ Token validation: ✓ Still valid
      └─ Form loads
      └─ User enters new password
      └─ Password hashed & saved
      └─ Token cleared from DB
      └─ Redirect to login.php
         │
  T0+1h: Same token becomes invalid
      └─ reset_token_expires < NOW()
      └─ User tries old email link
      └─ Error: "Link tidak valid atau sudah kadaluarsa"
         │
  T0+90m: User requests new reset
      └─ Process repeats...


════════════════════════════════════════════════════════════════════════════════

🔄 COMPARISON: MANUAL vs GOOGLE USERS
──────────────────────────────────────────────────────────────────────────────

┌──────────────┬──────────────────────┬──────────────────────┐
│   Property   │   Manual User        │   Google User        │
├──────────────┼──────────────────────┼──────────────────────┤
│ Registration │ Email + Password     │ Google Account       │
│ login_method │ 'manual'             │ 'google'             │
│ password DB  │ Hashed password      │ Random hash (unused) │
│ Forgot Pwd?  │ ✓ Can reset          │ ✗ Cannot reset       │
│ Reset Method │ Email link + token   │ Redirect to Google   │
│ Message      │ "Email dikirim..."   │ "Gunakan Google..."  │
│ Security     │ Token-based          │ OAuth-based          │
└──────────────┴──────────────────────┴──────────────────────┘


════════════════════════════════════════════════════════════════════════════════

✨ SYSTEM READY - ALL COMPONENTS INTEGRATED ✨

Feature: Password Reset with Smart Login Method Detection
Status: ✅ PRODUCTION READY
Version: 1.0
Date: January 21, 2026

════════════════════════════════════════════════════════════════════════════════
